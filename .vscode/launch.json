{
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "scc debug",
            "cargo": {
                "args": [
                    "build",
                ],
            },
            "args": [],
            "cwd": "${workspaceFolder}",
            "env": {
                "CARGO_MANIFEST_DIR": "${workspaceFolder}",
                "RUST_LOG": "debug"
            }
        }
    ]
}